services:
  - type: web
    name: my-streamlit-app
    env: python
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py
    plan: free
    pythonVersion: 3.9
